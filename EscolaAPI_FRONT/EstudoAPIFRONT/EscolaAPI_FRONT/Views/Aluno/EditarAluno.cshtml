@model EscolaAPI_FRONT.Models.Aluno

@{
    ViewData["Title"] = "EditarAluno";
}

<h1>EditarAluno</h1>

<h4>AlunoDTO</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="EditarAluno">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Nome" class="control-label"></label>
                <input asp-for="Nome" class="form-control " value="@Model.Nome" />
                <input type ="hidden" asp-for="IdAluno" class="form-control " value="@Model.IdAluno" />
                <span asp-validation-for="Nome" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
